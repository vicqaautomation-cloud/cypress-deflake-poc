{
  "name": "cypress-poc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:deflake": "deflake npx cypress run",
    "cypress:deflake": "npm run test:deflake",
    "typecheck": "tsc --noEmit",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "smoke": "./scripts/run-smoke.sh",
    "sanity": "TEST_ENV=qa npx cypress run --spec 'cypress/e2e/sanity/**/*.cy.ts'",
    "api": "TEST_ENV=qa npx cypress run --spec 'cypress/e2e/api/**/*.cy.ts'",
    "regression": "./scripts/run-regression.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cypress": "^15.9.0",
    "deflake": "^1.0.2",
    "prettier": "^3.8.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
